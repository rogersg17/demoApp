# Development environment for React frontend
# Backend API URL - the backend server that provides APIs
VITE_API_BASE_URL=http://localhost:3000

# Frontend development server runs on port 5173 (handled by Vite automatically)
# No need to configure frontend port here - Vite handles it via vite.config.ts
